{
  "_comment" : "Create a AWS AMI ith AMZ Linux 2018 with Java and Tomcat",
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-east-1",
    "source_ami": "ami-085925f297f89fce1",
    "instance_type": "t2.nano",
    "ssh_username": "ec2-user",
    "ami_name": "DevOpsClass-Build-{{isotime | clean_resource_name}}",
    "vpc_id": "vpc-04e69f2a4a09ae2e5",
    "subnet_id": "subnet-0365d6c1d3d38427f"
  }],
  "provisioners": [{
    "type": "shell",
    "inline": [
      "sleep 30",
      "sudo apt-get update -y",
      "sudo apt-get install nginx -y",
      "sudo service nginx start",
      "sudo chkconfig nginx on",
      "sudo apt-get install git -y ",
      "sudo apt-get install tree stress -y",
      "sudo git clone https://github.com/satya223/dynamodblock.git",
      "echo 'task is completed'"
      ]
  }]
}
